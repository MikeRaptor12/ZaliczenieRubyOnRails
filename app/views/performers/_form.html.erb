<%= form_with(model: performer) do |form| %>
  <% if performer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(performer.errors.count, "error") %> prohibited this performer from being saved:</h2>

      <ul>
        <% performer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :bio, style: "display: block" %>
    <%= form.textarea :bio %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
