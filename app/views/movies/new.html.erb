<h1>Dodaj nowy film</h1>

<%= form_with model: @movie, local: true do |form| %>
  <div>
    <%= form.label :title, 'Tytuł filmu' %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, 'Opis filmu' %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit 'Dodaj film' %>
  </div>
<% end %>
